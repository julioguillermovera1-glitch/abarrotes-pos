<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Iniciar sesión — Abarrotes POS</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-body">
  <div class="login-card">
    <img src="/img/icon.ico" alt="Abarrotes POS" style="width:64px; height:64px; border-radius:14px; margin-bottom:0.4rem">
    <h1>Abarrotes POS</h1>
    <p class="subtitle">Iniciar sesión</p>
    <% if (error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>
    <form method="POST" action="/login">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <label>Usuario</label>
      <input type="text" name="usuario" required autofocus>
      <label>Contraseña</label>
      <input type="password" name="password" required>
      <button type="submit" class="btn btn-primary btn-block">Entrar</button>
    </form>
    <p style="margin-top:0.8rem"><a href="/recuperar">¿Olvidaste tu contraseña?</a></p>
  </div>
</body>
</html>
